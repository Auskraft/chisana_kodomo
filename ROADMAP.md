# ROADMAP — Chisana kodomo · ちいさなこども

> **План, по которому идём.** Документ ведём живым: отмечаем `[x]` сделанное,
> двигаемся фазами сверху вниз. Краткий обзор проекта — в [`CLAUDE.md`](CLAUDE.md).
> Легенда статусов: ✅ готово · 🔜 следующий шаг · ⏳ позже.

## Как пользоваться
- Фазы идут **в порядке зависимостей** (нельзя строить игру без core).
- У каждой фазы: **цель**, **чек-лист** задач и **«готово, когда»** (DoD).
- Перед каждым коммитом — `flutter analyze` чисто + `flutter test` зелёные.
- Меняем план свободно: это наш рабочий инструмент, не закон.

## Сквозные принципы (всегда)
- **UX «без проигрышей»:** никаких таймеров/Game Over; ошибка → мягкое «попробуй
  ещё»; мгновенная похвала + звёзды/наклейки. Мягкий детский «сок», не аркада.
- **Адаптивная вёрстка:** размеры — доли от экрана (`LayoutBuilder`/`MediaQuery`
  + `clamp`), без абсолютных пикселей. Текст — через `textTheme`.
- **Цвета только из темы:** `context.appColors`, никаких хардкод-значений
  (готовимся к 5–10 темам, реестр `AppThemes.all`).
- **Ассеты по минимуму:** эмодзи + фигуры Flame; голос — системный TTS;
  звук — процедурно/CC0. Без рисованных ассетов до этапа бренда.
- **Движок:** Flame везде (единый канвас + переиспользуемый «сок»).
- **Пауза-нотифаер — `isPaused`** (не `paused`).
- **Git:** коммитим по ходу в `main`; пуш — в foreground (фон режет сеть).
- **Агент не собирает APK** — верификация только `analyze`/`test`; сборка у владельца.

---

## Фаза 0 — Каркас ✅
**Цель:** пустой, но «настоящий» проект, который запускается и проходит проверки.
- [x] `flutter create` (Android-only, org `com.auskraft`), label «Chisana kodomo»
- [x] Светлая тёплая тема как `ThemeExtension` + реестр тем `AppThemes.all`
- [x] Лобби-заглушка (маскот + витрина-тизер 5 игр), адаптивная вёрстка
- [x] `flutter analyze` чисто, smoke-тест зелёный
- [x] git + GitHub (`main`), `CLAUDE.md`, `ROADMAP.md`
- [x] Зафиксированы решения: Flame везде · пилот «Счёт» · оба режима по наборам
**Готово, когда:** приложение бутится в лобби, проверки зелёные, репо на GitHub. ✅

## Фаза 1 — Фундамент `core/` (общий для всех игр) ✅
**Цель:** общие сервисы, на которых стоит любая игра. Перенос из tanks/yoru,
перекраска в яркое, адаптация «под детей».
- [x] `core/theme` — палитра/тема (готово в Фазе 0)
- [x] deps: `flame`, `flutter_tts`, `shared_preferences`, `flutter_displaymode`
- [x] `core/storage/GameStorage` — открытые наборы + звёзды по играм, выбранная
      тема, согласие, настройки (звук/голос/вибро). Юнит-тесты. (Наклейки — Фаза 4.)
- [x] `core/feedback/haptics` — мягкие вибро (тап/выбор/нежный «успех»)
- [x] `core/audio/Sfx` — фасад событий (no-op до Фазы 5)
- [x] `core/voice/Voice` — `flutter_tts`: рус. речь `ru-RU`, помедленнее, фолбэк
      no-op (без TTS/в тестах — молчит)
- [x] `core/praise/Praise` — фразы похвалы + звёзды за набор (чистая логика + тест)
- [x] `core/components/overlay_kit` — старт-панель, панель «Молодец!» со звёздами
      (вместо Game Over), пауза, чипы HUD. Адаптивные, цвета из темы.
**Готово, когда:** core подключён, покрыт тестами где есть логика, `analyze`/`test`
зелёные. ✅ (Родительский гейт/legal/rating — в Фазе 6, когда появятся внешние ссылки.)

## Фаза 2 — Пилот «Счёт» (вертикальный срез) ✅ код / ⏳ проверка на устройстве
**Цель:** первая **играбельная** игра end-to-end — обкатать 4 слоя на живом Flame.
- [x] `logic/counting_logic.dart` — наборы, оба режима, исходы (+ 12 тестов)
- [x] `game/counting_flame_game.dart` — Flame-канвас: эмодзи-объекты, тап-счёт и
      выбор цифры, «сок» (конфетти/поп/мягкая тряска), `isPaused`
- [x] `ui/counting_overlays.dart` — HUD (набор/раунд/пауза); старт/похвала/пауза —
      из `overlay_kit`
- [x] `counting_game_screen.dart` — хост: голос-подсказки, запись прогресса
      (наборы/звёзды), навигация по наборам
- [x] Лобби: карточка «Счёт» активна + навигация; остальные притушены («скоро»)
**Готово, когда:** `analyze`/`test` зелёные ✅; «Счёт» играется на устройстве,
голос + «сок» на месте — **проверяет владелец** (агент не запускает). Это шаблон
для всех следующих игр.

## Фаза 3 — Остальные игры (по шаблону пилота) ✅ код / ⏳ «фил» на устройстве
**Цель:** довести до 6 игр. Каждая = те же 4 слоя (logic+тесты → Flame → overlays
→ host → карточка в лобби).
- [x] **Парочки** (память) — переворот карточек на совпадение; колода (символ ×2,
      детерминизм), блок на несовпадении, звёзды масштабируются числом пар; 9 тестов
- [x] **Цвета и формы** — тап по такой же фигуре/цвету (наборы: цвет → форма →
      оба); фигуры рисуются в Flame; 6 тестов логики
- [x] **Звуки животных** — «найди зверя» по голосу; генерация раунда (уникальные
      варианты, один верный); 6 тестов. Звук зверя — Фаза 5 (`Animal.soundKey`),
      сейчас эхо имени голосом
- [x] **Музыка / ксилофон** — свободная игра-инструмент: 8 радужных пластин,
      тап → отскок/вспышка/вибро; частоты нот заданы (`Xylophone.frequency`),
      4 теста. Тон пластин — Фаза 5 (процедурно)
- [x] **Раскраска** — творческая студия, **3 режима за переключателем:** заливка ·
      свободное рисование пальцем (DragCallbacks) · по номерам (хит-тест Path).
      Картинки = фигуры в коде (Домик, Цветок). Без проигрышей: «Готово! Красиво!».
      Тестируема модель (области/палитра/цели/завершение) — 6 тестов; сам
      холст/хит-тест/рисование — на устройстве
**Готово, когда:** все 6 играбельны ✅, единый стиль/«сок», `analyze` чисто +
`test` зелёные (51) ✅. «Фил» (анимации/голос/хит-тест/рисование) — проверяет
владелец на устройстве (агент не запускает).

## Фаза 4 — Мета и мягкое удержание ✅ (тема/стрик — позже)
**Цель:** ребёнку хочется возвращаться — без давления.
- [x] Экран наград: суммарные звёзды + коллекция наклеек (пороги 1..51);
      `features/rewards` (вход — пилюля-звёзды в лобби); 5 тестов логики
- [x] Прогресс по наборам: открытие следующего без провала + выбор набора в лобби
      (`set_picker_screen`, открытые доступны, будущие с замком)
- [x] Настройки: тумблеры звук/голос/вибро + выбор голоса ✅; **сброс прогресса** ✅
      (за родительским гейтом, Фаза 6); выбор темы (5–10) — позже
- [ ] (Опц.) мягкий «возвращайся завтра» — стрик без штрафов
**Готово, когда:** прогресс виден и радует ✅, `analyze`/`test` зелёные ✅.
(Выбор темы из 5–10 — отложено до финальных палитр владельца.)

## Фаза 5 — Звук и голос (контент) ✅ код / ⏳ контент (CC0, голос-пак — владелец)
**Цель:** наполнить аудио-слой.
- [x] Процедурные джинглы (Python+wave): `tool/gen_sfx.py` → `assets/sfx/*.wav`
      (tap/correct/soft/star/complete/start); бэкенд `core/audio/sfx.dart` играет их
- [x] Ноты для «Музыки»: `tool/gen_notes.py` → `assets/notes/note_0..7.wav`;
      полифонический `core/audio/sound_pool.dart`
- [~] CC0-звуки животных (Freesound): хук `onAnimalSound` + `assets/animals/`
      подключены; **сами файлы кладёт владелец** (`assets/animals/README.txt`).
      Пока — имя зверя голосом (тихий фолбэк). Атрибуции → `docs/ATTRIBUTIONS.md`
- [~] Причесать TTS (скорость/паузы/похвала) — на устройстве у владельца
- [~] **Голос:** очередь ✅ + выбор в Настройках ✅ + клипы (фолбэк на TTS) ✅.
      Остаётся сгенерить пак `tool/gen_voice_pack.py` (Silero); названия
      цветов/фигур/зверей в пак — позже (TTS их уже произносит)
**Готово, когда:** звук/голос работают; без файлов — тихий фолбэк ✅ (код готов).

## Фаза 6 — Приватность и родительский контроль ✅
**Цель:** безопасность для детей и соответствие правилам сторов.
- [x] `core/parent_gate` — гейт (умножение 3..9, выбор из 4) перед внешними
      действиями; чистая логика + 3 теста
- [x] `core/legal` — политика (офлайн, без сбора данных) + экран согласия при
      первом запуске (`RootGate` в `main`) + экран политики
- [x] `core/rating` — «Для родителей» за гейтом: почта (mailto), оценка (URL
      стора заполнить при публикации), политика, сброс прогресса, версия
- [x] Заявлено: без рекламы/трекинга, офлайн, 0+ — см. `STORE_DESCRIPTION.md`
**Готово, когда:** внешние действия за гейтом ✅, документы на месте ✅, `analyze` чисто ✅.

## Фаза 7 — Бренд-ассеты ⏳
**Цель:** лицо приложения.
- [ ] Иконка (из 9 вариантов → adaptive icon) + сплэш
- [ ] Маскот-арт (1 шт) — лобби/награды
- [ ] Шрифт при необходимости (сейчас системный)
**Готово, когда:** иконка/сплэш/маскот в сборке, генераторы прогнаны.

## Фаза 8 — Публикация (RuStore, опц. Google Play) ⏳
**Цель:** релиз.
- [x] `STORE_DESCRIPTION.md` (рус. названия, описание, теги, форма «безопасность
      данных», чек-лист) — готов
- [ ] Скриншоты
- [ ] Подпись релиза (keystore), versionCode/versionName
- [ ] Чек-лист модерации (детская категория, 0+)
**Готово, когда:** AAB/APK подписан и принят в стор.

---

## Бэклог — ещё 2 игры (после прохода по правкам)
Витрина станет **8** (сетка 3+3+2, ровно). Обе — по существующему 4-слойному
шаблону, «без проигрышей», «сок» как у всех.

- 🐮 **Ферма / «Кто как говорит?»** (`features/farm`) — игрушка free-play, как
  «Музыка» (фазы ready→playing, без наборов/звёзд). Тап по зверю → отскок + голос
  произносит звукоподражание (TTS — **работает офлайн уже сейчас**) + CC0-звук
  `assets/animals/<key>.wav`, когда положат. **Правка модели:** добавить поле
  `says` в `Animal` (му-у / гав-гав / мяу / и-го-го / ква-ква …). Реюз:
  `Animals.all`, `SoundPool`, `Voice`. id `farm` (не путать с квизом `animals`).
- 🧩 **Что лишнее? (Логика)** (`features/odd_one_out`) — квиз с наборами/звёздами,
  как «Цвета и формы»/«Звуки животных». Показать N предметов, один лишний по
  признаку (цвет → форма → категория), тап по нему → похвала; неверный — мягко.
  Логика генерации (группа «своих» + один «чужой», ровно один верный) —
  тестируемая. Выбор набора в лобби (как у игр со звёздами).
- Каждой: logic+тесты → Flame → overlays → host → карточка в лобби. Добавить в
  `RewardsCatalog` (для «Что лишнее» — звёзды считаются в наградах).

## Что сейчас
- ✅ Фазы 0–6 (код): **все 6 игр** + награды/наклейки + открытие наборов + звук
  (SFX/ноты) + согласие/политика/родительский гейт/зона родителей. `analyze` чисто,
  **60 тестов**. `STORE_DESCRIPTION.md` готов.
- 🔜 **Следующее (в основном владелец):** Фаза 7 (арт: маскот, сплэш — иконка есть),
  Фаза 8 (скриншоты, keystore, заливка в стор). Проверка «фила» всех игр на устройстве.
- 🧾 Тех-долг / контент владельца: пуш на GitHub (фоны 16 МБ + аудио), CC0-звуки
  зверей (`assets/animals/`), голос-пак (`tool/gen_voice_pack.py`), URL стора в
  `ParentZoneScreen.storeUrl`, WebP-сжатие фонов, выбор темы (5–10), финальный
  нейминг игр + иконки, больше картинок в «Раскраске».
